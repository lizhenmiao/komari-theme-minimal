# 复制成 .env.local，填你自己的 Komari 实例地址。
# dev server 会把 /api 和 /themes 代理到这里，WebSocket 也一并代理。
#
# 开发时直接打开 http://localhost:5273/ 即可，页面路径和线上一致
# （/、/instance/xxx）。带 /themes/{short}/dist/ 前缀的只有构建产物里的
# 资源引用，开发态不需要在地址栏里输入这段前缀。
VITE_API_TARGET=http://127.0.0.1:25774
