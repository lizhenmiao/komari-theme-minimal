/**
 * Scaffold placeholder. Routing, the transport layer and the pages replace this
 * body in the implementation milestones; it exists so the build and the package
 * contract can be verified end to end first.
 */
export default function App() {
  return (
    <main className="km-main grid min-h-dvh place-items-center">
      <p className="text-sm text-neutral-500">minimal — scaffold only</p>
    </main>
  )
}
